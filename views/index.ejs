<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./index.css">
    <title>Url Sort</title>
</head>
<body>
    <h1 id="heding">Url Sortner</h1>

    <% if (locals.error) { %>
     <p class="error"> <%= error %> </p>
    <% } %>

    <form action="/" method="post">
        <input type="text" name="url" id="url" placeholder="Enter your  Url">
        <input type="submit" name="submit-url" id="url-btn" >
    </form>

    <div class="short-url">
        <% if (locals.short_url) { %>
         <a href="<%= short_url %>" class="short_url"><%= short_url %> </a>
        <% } %>
    </div>
</body>
</html>